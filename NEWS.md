# utop (development version)

# utop 0.1.1

- Removed `sp` and `spacetime` support. utop now relies on `sf` and
  `stars` only, as a stand-alone package.

# utop 0.1.0

- Introduced universal top-kriging.
